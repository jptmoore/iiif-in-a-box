services:
  nginx:
    container_name: iiif-nginx-maintenance
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./conf/maintenance.conf:/etc/nginx/conf.d/default.conf:ro
      - ../templates/maintenance.html:/usr/share/nginx/html/index.html:ro
    networks:
      - iiif-network

networks:
  iiif-network:
    external: true
