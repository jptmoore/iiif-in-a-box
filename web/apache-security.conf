# Apache security configuration
# Disable directory browsing for security
<Directory "/usr/local/apache2/htdocs">
    Options -Indexes
</Directory>

# Block direct access to images directory - images should only be accessed through Cantaloupe
<Directory "/usr/local/apache2/htdocs/images">
    Require all denied
</Directory>